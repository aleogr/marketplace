# F15 — Roles, permissions, owner bootstrap and the console: design

**Date:** 2026-09-26
**Status:** agreed with the owner, not yet implemented
**Delivers:** roadmap F15 (`docs/roadmap.md`), against `docs/requirements.md` §19 and §27, §18.2's
staff rules, and `docs/design.md` §2.6 and §4.

The platform gets its first staff: the owner, created once through a secure first run, and the
colleagues the owner invites. They sign in to a console on its own subdomain, always with a second
factor, and see only what their roles permit, in the marketplaces their roles cover. Roles are made of
permissions declared in the code. Every change to who may do what is audited. The console's shell shows
the deployed build.

## What exists and what that means for this delivery

F13 and F14 built accounts, sessions, passwords, second factors, the step-up and the failure counter,
all per marketplace. `account` already allows `kind = 'staff'` with a null `marketplace_id`, and
`identity.Policy` already requires staff to have an app or a key and refuses e-mail codes to them. What
is missing is a way for a staff account to exist and sign in: every identity table's row-level policy is
`marketplace_id = current_marketplace_id()`, which a null marketplace never satisfies, and every identity
flow opens its transaction for a marketplace. The console does not exist; neither do roles or
permissions. The build identifier is embedded and logged, but not shown.

## Decisions

**D1. The console lives on its own subdomain.** The owner chose this on 2026-09-26 over serving it on
the platform host. `CONSOLE_HOST` is configured like `PLATFORM_HOST`; tenancy resolves it to a third
kind of host, `ConsoleHost`, which serves the console and its sign-in only. The platform host and the
marketplace hosts never serve the console, and the console host serves no marketplace. Cookies are host
cookies (`__Host-`), so the console's session is never sent to a store and a store's never to the
console. Terraform adds the Cloud Run domain mapping; the DNS record in Cloudflare is a manual step for
the owner.

**D2. The bootstrap token is generated by Terraform, not by the service.** The owner chose this on
2026-09-26 over the roadmap's "generated at start and written to the log and to Secret Manager".
Terraform creates a random token (`random_password`, 32 bytes of entropy) as a Secret Manager secret, the
same way it creates the mail webhook token; Cloud Run hands it to the service as an environment variable;
the service only reads it. The token never appears in a log, the runtime account gets no permission to
write secrets, and several instances agree on the one token. It is used once: its SHA-256 is recorded
when the owner is created, and `/setup` refuses a used token and answers "not found" once an owner
exists. The owner reads the token with a `gcloud secrets versions access` command.

**D3. Staff read tenant data under row-level security.** The owner chose this on 2026-09-26 over the
design's "staff use a database role without RLS". A console route that shows a marketplace's data names
the marketplace; authorization checks that one of the staff member's roles grants the permission in that
marketplace or across the platform, and only then opens the transaction for that marketplace
(`InTxFor`). A missing filter can therefore never show another marketplace's rows. A view across the
platform walks the marketplaces the staff member may see, one transaction each. `docs/design.md` §2.6 is
amended.

**D4. Staff are accounts without a marketplace, through the same identity code.** Staff reuse F13 and
F14: argon2id passwords, server sessions, second factors, the step-up, the challenge and the failure
counter. The identity tables' row-level policies become `marketplace_id IS NOT DISTINCT FROM
current_marketplace_id()`, so a transaction that names no marketplace sees the platform's rows (staff)
and nothing else, and a marketplace's transaction still sees only its own. The identity service gains a
platform visit (no marketplace) that opens its transactions that way. Staff must have an app or a key:
a staff account that has none is sent to enrol one before any other console page, and e-mail codes stay
refused to staff (F14). Staff never sign in on a store's host, and buyers never on the console's.

**D5. Staff join by invitation.** The owner chose this on 2026-09-26 over a temporary password. A staff
member with the permission to invite gives a name, an e-mail address and roles; the invitee receives a
link, valid for 7 days and usable once, where they set their own password and enrol an app or a key
before reaching the console. Nobody else ever knows their password. The invitation's token is stored as
its SHA-256.

**D6. Permissions are declared in the code; roles are made in the console.** The registry is a Go list
of permissions, each with a key and a label in both languages. The migrator synchronises the
`permission` table with it on every deploy (adds, and removes what the code no longer declares), and
`role_permission` references it, so a permission cannot exist in the database without existing in the
code. The owner combines permissions into roles and assigns a role to a staff member with a scope: one
marketplace or the whole platform. The permissions of this delivery:

| key | allows |
|---|---|
| `console.view` | signing in to the console and seeing its home |
| `console.version` | seeing the build identifier on `/console/version` |
| `staff.invite` | inviting a staff member |
| `staff.deactivate` | deactivating a staff member |
| `staff.reset_factor` | resetting a colleague's second factor |
| `roles.manage` | creating, editing and deleting roles and assigning them |

**D7. The owner role is protected in the service and in the database.** It has every permission,
including those added later (it is checked by the role, not by rows); it cannot be deleted, edited or
assigned to anyone but the owner; there is exactly one owner, with the platform as scope. The service
refuses such changes, and the database refuses them again through constraints and a trigger, so a
mistake in the code cannot strip the owner.

**D8. A request without the permission sees "not found".** Authorization answers 404, not 403, so a
console page does not reveal it exists to someone who may not see it. The menu shows only what the
staff member's roles permit.

**D9. Staff recovery and offboarding are in this delivery.** The owner chose both on 2026-09-26.
- **Resetting a colleague's second factor** (`staff.reset_factor`, §18.2): the colleague loses every
  method and recovery code and every session, and must enrol a new factor at the next sign-in; they are
  told by e-mail. Nobody resets their own factor this way (their recovery codes are for that), and only
  the owner changes the owner's factors.
- **Deactivating a staff member** (`staff.deactivate`): the account is marked deactivated, its sessions
  end and its roles are removed; a sign-in with it is answered like a wrong password; they are told by
  e-mail. The owner cannot be deactivated. Reactivation is not in this delivery.

**D10. Sensitive console actions ask for the second factor again.** Creating, editing, deleting or
assigning a role, inviting, deactivating and resetting a factor go through the F14 step-up, with its
ten-minute window.

## Data model

| table | columns that matter |
|---|---|
| `permission` | `key` (primary key), synchronised from the code; the labels live in the locale files |
| `role` | `id`, `name`, `owner` (true only for the owner role; at most one row), `created_at` |
| `role_permission` | `role_id`, `permission_key` (references `permission`) |
| `user_role` | `account_id` (a staff account), `role_id`, `marketplace_id` (null for the whole platform), `assigned_by`, `assigned_at` |
| `bootstrap` | `token_hash`, `used_at`, `owner_id`: at most one row, written when the owner is created |
| `staff_invitation` | `id`, `token_hash`, `email`, `name`, `roles` (the role and scope pairs), `invited_by`, `expires_at`, `used_at` |
| `account` (F13) | gains `deactivated_at`; the owner is the staff account holding the owner role |

These tables belong to the platform (no marketplace) and are read only by the console's platform
transactions; the owner role's rules are enforced by a partial unique index (one owner role; one
assignment of it) and a trigger refusing to delete or change the owner role, delete its assignment or
change its permissions. The identity tables' policies change as D4 says.

## Flows

**First run.** While no owner exists, the console host serves `/setup`: the token, a name, an e-mail
address and a password (F13's rules, the breach check included). A right token creates the staff
account, the owner role and its assignment, records the token as used and signs the owner in; the owner
is sent to enrol an app or a key, sees the recovery codes, and only then reaches the console. A wrong
token is refused under a named rate limit (`setup`). Once an owner exists, `/setup` is "not found".

**Staff sign-in.** On the console host only: the password, then the second factor (F14's challenge),
then the console. A staff account without a factor is sent to enrol one. A deactivated account is
answered like a wrong password.

**Roles.** A list of roles with their permissions; creating and editing a role (a name and a set of
permissions); deleting a role no one holds; assigning a role to a staff member with a scope and removing
it. The owner role shows as protected.

**Invitation.** A form (name, e-mail, roles with scopes) sends the invitation mail; the link opens a page
to set the password and enrol a factor; the invitation is then used. An expired or used link is "no
longer valid".

**Staff list.** The staff members with their roles and the state of their second factor; the actions to
reset a factor and to deactivate, each with a confirmation and a step-up.

**Console shell.** Its own layout: the header with the staff member's name, the language switch and
sign-out; the menu built from permissions; the footer with the build identifier. `/console/version`
answers the build identifier as JSON to whoever holds `console.version`.

**Mail** (en-US and pt-BR, text and HTML): the invitation, a factor reset, a deactivation.

**Audit:** the owner's creation; each role created, edited or deleted; each assignment and removal;
each invitation sent and accepted; each deactivation; each factor reset, with the staff member as the
actor (`audit.Staff`).

Every page, message and e-mail exists in en-US and pt-BR.

## Testing

**Integration** (real PostgreSQL, as the application role under RLS): a staff member scoped to
marketplace A cannot read marketplace B; the owner role cannot be deleted, edited, reassigned or
stripped, by the service or by SQL; `/setup` is gone after the bootstrap; the token cannot be used twice;
a staff member without `console.version` gets 404 on the version endpoint; a buyer transaction sees no
staff rows and a platform transaction no buyer rows; an expired or used invitation is refused; a
deactivated account cannot sign in; a factor reset ends the sessions and forces enrolment; the permission
table matches the code after a migration.

**End-to-end** against a fresh database: the whole bootstrap (token, owner, factor, recovery codes);
creating a role, inviting a staff member with it for one marketplace, accepting the invitation, signing
in as that staff member and asserting the menu shows only what the role permits; screenshots of the
console in both languages; the audit records the run produced.

## Pull requests

Each is green and useful on its own.

1. **The console host, staff accounts and the owner.** `CONSOLE_HOST` and its domain mapping, the
   bootstrap secret in Terraform, the identity policies for platform rows, the platform visit, `/setup`,
   staff sign-in with the mandatory factor, the console shell with the build identifier, and
   `/console/version` (held by the owner until roles exist).
2. **Roles and permissions.** The registry and its synchronisation, the roles and assignments with the
   owner role's protections, the authorization middleware, the menu, and the role pages.
3. **Invitations, deactivation and factor reset**, with their mails and step-ups.
4. **Closing.** The end-to-end run from a fresh database, screenshots, the roadmap's F15 ticked.

**What is needed from the owner**, one at a time, once PR 1 is deployed to the lab: create the DNS record
of the console's subdomain in Cloudflare; read the bootstrap token with the command provided; open
`/setup` and create the owner; enrol an app or a key and keep the recovery codes; confirm a sign-in.

## Out of this delivery

Reactivating a deactivated staff member; editing a staff member's name or e-mail; an audit viewer in the
console; console parameters and the marketplace wizard (later deliveries); sessions listing and "sign out
everywhere"; staff notices in an account-level language.

## Risks

**The owner loses every factor and every recovery code.** Nobody can reset the owner's factor through
the console. The recovery is a manual database procedure, documented in `docs/infrastructure.md`,
performed by someone with the migrator's access.

**Changing the identity policies.** Moving every identity table from `=` to `IS NOT DISTINCT FROM`
must not let a marketplace see platform rows or the reverse; the integration tests assert both
directions for every table touched.

**The console subdomain needs DNS before it answers.** Until the owner creates the record, the lab's
console is unreachable; nothing else depends on it.
